

########## DATA
data_dir: "datasets/synthetic/leo_will_verysmall/"
prefix: "10_examples"
train_suffix: ".train"
dev_suffix: ".dev"
test_suffix: ".test"

sos: "<s>"
eos: "</s>"
unk: "<unk>"

# should be in left-right order
data_spec:
  - name: "text-input"
    type: "text"

  # - name: "continuous_1"
  #   type: "continuous"
  #   reverse_gradients: False

  # - name: "continuous_2"
  #   type: "continuous"
  #   reverse_gradients: False

  # - name: "categorical_1"
  #   type: "categorical"
  #   reverse_gradients: False
# TODO FIGURE OUT HOW TO ADD/RM VARS PAINLESSLY
  - name: "categorical_2"
    type: "categorical"
    reverse_gradients: False


########## MODELING
seed: 1
working_dir: "test_run"

# points to a 1-per-line file of tokens, or null (use all words)
vocab: null 

# all of these will be run
model_spec:
  - type: "neural"
    params:
      batch_size: 16
      num_train_steps: 10000
      learning_rate: 0.001
      embedding_size: 16
      encoder_layers: 1
      encoder_units: 16
      attn_layers: 1
      attn_units: 16
      classifier_layers: 1
      classifier_units: 16
      regressor_layers: 1
      regressor_units: 16
      gradient_clip: 5.0
#      TODO

  - type: "mixed-regression"
    params:
      # TODO

  - type: "l1-regression"
    params:
      # TODO

  - type: "l2-regression"
    params:
      # TODO

